<% if !@category.errors.empty? %>
  <% @category.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>
<%= form_for @category do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit %>
<% end %>
